# Environment: development, production (default: production)
# When set to development, API key authentication is disabled
ENVIRONMENT=production

# API key for securing API endpoints in non-development environments
# If not set, API endpoints will be accessible without authentication
API_KEY=your-secure-api-key-here